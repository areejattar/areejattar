import { products, collectionMeta, type Collection } from "@/data/products"; import CatalogueBrowser from "./CatalogueBrowser";
export default function CollectionPage({collection}:{collection:Collection}){const m=collectionMeta[collection]; return <main className={`collection-page theme-${collection}`}><section className="collection-hero"><div className="collection-art" aria-hidden="true"/><div className="collection-copy"><p className="eyebrow">AREEJ / {collection}</p><h1>{m.title}</h1><p className="collection-subtitle">{m.subtitle}</p><p>{m.description}</p></div></section><section className="collection-products"><div className="section-heading"><div><p className="eyebrow">Catalogue</p><h2>Find your signature.</h2></div></div><CatalogueBrowser products={products.filter(p=>p.collection===collection)}/></section></main>}
